/**
 * Setting-up root level plugins
 */
plugins {
    id 'org.sonarqube' version "$sonarqubeVersion"
}


apply from: "$rootDir/gradle/projects.gradle"

ext['postgresql.version'] = "$postgresVersion"
