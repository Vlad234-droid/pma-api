<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tesco.pma.feedback.dao.FeedbackItemDAO">

    <insert id="insert" flushCache="true" useGeneratedKeys="true" keyColumn="uuid" keyProperty="uuid">
        INSERT INTO feedback_item (uuid, code, content, feedback_uuid)
        VALUES (#{feedbackItem.uuid}, #{feedbackItem.code}, #{feedbackItem.content}, #{feedbackItem.feedbackUuid})
    </insert>

    <update id="update">
        UPDATE feedback_item
        SET
            code = #{feedbackItem.code},
            content = #{feedbackItem.content}
        WHERE uuid = #{feedbackItem.uuid}
    </update>
</mapper>